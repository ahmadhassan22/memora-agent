services:
  memora-api:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: memora-api
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    volumes:
      - chroma_data:/app/data
    restart: unless-stopped

volumes:
  chroma_data: